<Window x:Class="PhoneBook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Телефонная книга (Shell)" Height="500" Width="600"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <!-- Меню навигации вверху окна -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Background="#F0F0F0">
            <Button Content="Контакты"
                    Command="{Binding ShowContactsCommand}"
                    Margin="5" Padding="12,4"/>
            <Button Content="О программе"
                    Command="{Binding ShowAboutCommand}"
                    Margin="5" Padding="12,4"/>
        </StackPanel>

        <!-- Область отображения текущего экрана -->
        <ContentControl Content="{Binding NavigationService.CurrentViewModel}" />
    </DockPanel>
</Window>
