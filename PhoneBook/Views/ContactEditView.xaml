<UserControl x:Class="PhoneBook.Views.ContactEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="Редактирование контакта"
                   FontSize="20" FontWeight="Bold" />

        <TextBlock Text="Имя:" Margin="0,15,0,0" />
        <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Padding="3"/>

        <TextBlock Text="Телефон:" Margin="0,5,0,0" />
        <TextBox Text="{Binding EditPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Padding="3"/>

        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Padding="5"/>
        </StackPanel>
    </StackPanel>
</UserControl>